<!DOCTYPE html>
<html lang="pt-br">
  <%- include('./partials/head.ejs') %>
  <body>
    <!-- <% const message = 'Hello World' %>
    <h1><%= message %></h1> -->
    <%- include('./partials/navbar.ejs')%>
      <div class="content">
        <% if (users.length > 0) { %> <% users.forEach(user => { %>
        <h3 class="title"><%= user.firstName %> <%= user.lastName %></h3>
        <p><%= user.email %></p>
        <% }) %> <% } else { %>
        <p>Não existe usuário para exibir</p>
        <% } %>
      </div>
  </body>
</html>
